<!DOCTYPE html>
<html>
  <head>
    <%- include('head.ejs') %>
  </head>

  <body>
    <h1 class="container mt-5">Happy bank</h1>
    <h4 class="container mt-5"><strong>회원가입</strong></h4>
    <div class="container mt-5">
      <form action="/auth/signup" method="POST">
        <div class="form-group">
          <label>아이디</label>
          <input
            type="text"
            placeholder="4~24자의 알파벳 또는 숫자의 조합"
            class="id_input"
            name="id"
            pattern="[A-Za-z0-9]{4,24}"
            required
            check_result="fail"
            title="8~16자의 알파벳 또는 숫자의 조합이어야 합니다."
          />
          <button type="button" class="id_check_button btn btn-secondary">
            중복확인
          </button>
          <i
            class="fas fa-solid fa-check id_check_success"
            style="display: none"
          ></i>
          <p class="id_check_success" style="display: none">확인 완료</p>
        </div>

        <div class="form-group">
          <label for="password" class="form-label">비밀번호</label>
          <input
            type="password"
            class="form-control"
            name="password"
            id="password"
            pattern="^(?=.*[A-Za-z0-9]).{8,16}$"
            required
            title="8~16자의 알파벳 또는 숫자의 조합이어야 합니다."
            placeholder="8~16자의 알파벳 또는 숫자의 조합"
            onchange="passwordSame()"
            check_result="fail"
          />
          <span id="password_alert"></span>
        </div>

        <div class="form-group">
          <label for="passwordCheck" class="form-label">비밀번호 확인</label>
          <input
            type="password"
            class="form-control"
            id="passwordCheck"
            placeholder="8~16자의 알파벳 또는 숫자의 조합"
            pattern="^(?=.*[A-Za-z0-9]).{8,16}$"
            required
            title="8~16자의 알파벳 또는 숫자의 조합이어야 합니다."
            onchange="passwordSame()"
          />
          <span style="color: red" id="passwordCheck_alert"></span>
        </div>

        <div class="form-group">
          <label>이메일</label>
          <input class="email_input" name="email1" type="text" required />
          @
          <input
            class="email_input"
            name="email2"
            type="text"
            pattern="\S+\.\S+"
            required
          />
          <select onChange="selectEmail()" id="select-email">
            <option value="1" selected>선택하세요</option>
            <option value="naver.com">naver.com</option>
            <option value="gmail.com">gmail.com</option>
            <option value="hanmail.net">hanmail.net</option>
            <option value="daum.net">daum.net</option>
            <option value="1">직접입력</option>
          </select>
        </div>

        <div class="form-group">
          <label for="nickname" class="form-label">닉네임</label>
          <input
            type="input"
            class="form-control"
            name="nickname"
            id="nickname"
            placeholder="16자 이내의 알파벳 또는 숫자의 조합"
            pattern="^(?=.*[A-Za-z0-9]).{1,16}$"
            required
            title="16자 이내의 알파벳 또는 숫자의 조합이어야 합니다."
          />
          <span id="nickname_alert"></span>
        </div>

        <div class="container mt-5">
          <button type="submit" class="btn btn-primary">회원가입</button>
        </div>
      </form>
    </div>

    <script type="text/javascript" src="/public/javascripts/auth.js"></script>
  </body>
</html>
