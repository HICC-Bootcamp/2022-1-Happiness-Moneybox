<!DOCTYPE html>
<html>
  <head>
    <%- include('head.ejs') %>
  </head>
  <body>
    <div id="picture">
      <img src="public/images/pig.jpg" id="picture-inside" />
      <div id="total-money"><%= money %>원</div>
    </div>
    <div id="list">
      <div class="mt-5 last-day">
        <h4 class="text-center"><%= user.nickname %>님,</h4>
        <h4 class="text-center">
          D-day까지 <span id="d-day"></span>일 남았습니다.
        </h4>
      </div>
      <div class="table-wrapper mt-5">
        <table class="table">
          <thead>
            <tr class="table-secondary">
              <th scope="col">Date</th>
              <th scope="col">Title</th>
              <th scope="col">Happy money</th>
              <th class="delete-on" scope="col">Delete</th>
            </tr>
          </thead>
          <tbody>
            <% var i; %> <% for(i=0; i < posts.length; i++){ %>
            <tr>
              <td><%= posts[i].date %></td>
              <td>
                <a class="post" href="#"><%= posts[i].title %></a>
              </td>
              <td><%= posts[i].money %></td>
              <td class="delete-on">
                <button type="button" class="btn btn-danger delete">X</button>
              </td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
      <button type="button" class="btn btn-primary mt-5 btn-list delete-send">
        Delete
      </button>
      <button type="button" class="btn btn-primary mt-5 btn-list add">
        Add
      </button>
      <div class="mt-4 dday-congratulation">
        <h4 class="text-center">축하합니다!</h4>
        <h4 class="text-center">
          <span id="year"></span>년 <%= user.nickname %>님은
        </h4>
        <h4 class="text-center">행복한 기억을 <%= i %>개,</h4>
        <h4 class="text-center">총 <%= money %>원 저금하였습니다.</h4>
      </div>
    </div>

    <script type="text/javascript" src="/public/javascripts/posts.js"></script>
  </body>
</html>
